<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "Teachme", instructors_path, class: "navbar-brand" %>
    </div>
    <div id="navbar" class="navbar-collapse collapse" aria-expanded="false" style="height: 1px;">
      <ul class="nav navbar-nav"> 
      <!-- Nav bar links -->
				<% if current_user != nil && current_user.instructor %>
				  <!-- < if current_user.instructor %> -->
			      <li><%= link_to "#{current_user.first_name.capitalize}'s Teacher Profile", instructor_path(current_user.instructor.id) %></li>
				  <% end %>

	      <% if current_user != nil && !current_user.instructor %>
			    <li><%= link_to "#{current_user.first_name.capitalize}'s Page", user_path(current_user) %> </li>
			  <% end %>

			  <% if current_user %>  
			    <li><%= link_to "Logout", session_path("current"), :method => :delete %></li>
				<% end %>

				<% if current_user == nil %>
				 <li><%= link_to "Login", new_session_path %></li>
				<% end %>
				
				<% if !current_user %>
				 <li><%= link_to "Sign up", new_user_path %></li>
				<% end %>
	      
	      <li><%= link_to "About", pages_about_path %> </li>

	      <li><%= link_to "Contact", pages_contact_path %> </li>

	      <% if current_user != nil && !current_user.instructor %>
					  <li><%= link_to 'Wanna Teach?', new_instructor_path %></li>
			  <% end %>
       </ul>

    </div><!--/.nav-collapse -->
  </div>   
</nav>